{
    "name": "a-tracking",
    "version": "1.0.0",
    "directories": {
        "test": "tests"
    },
    "author": "",
    "license": "ISC",
    "private": true,
    "dependencies": {
        "aws-sdk": "^2.437.0"
    },
    "devDependencies": {
        "aws-sdk-mock": "^5.1.0",
        "jest": "^24.7.1"
    },
    "scripts": {
        "build": "sam build",
        "deploy": "sam deploy",
        "test": "jest"
    }
}
